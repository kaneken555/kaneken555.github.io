---
const profile = {
  name: "kaneken555",
  role: "Web Developer",
  tagline: "シンプルで使いやすいUIを作ります。",
  location: "Japan",
  links: {
    github: "https://github.com/kaneken555",
    email: "mailto:your-email@example.com",
    // x: "https://x.com/xxxxx",
  },
};

// 作品はまずは直書きでOK。増えたら別ファイル化 or Content Collectionsへ移行できます。
const works = [
  {
    title: "Project A",
    description: "◯◯を解決するためのWebアプリ。",
    tech: ["Astro", "TypeScript", "CSS"],
    demo: "https://example.com",
    repo: "https://github.com/kaneken555/project-a",
  },
  {
    title: "Project B",
    description: "学習用に作った◯◯。",
    tech: ["React", "Vite"],
    demo: "",
    repo: "https://github.com/kaneken555/project-b",
  },
  {
    title: "Project C",
    description: "API連携のサンプル。",
    tech: ["JavaScript", "API"],
    demo: "",
    repo: "https://github.com/kaneken555/project-c",
  },
];

const skills = [
  { group: "Frontend", items: ["HTML", "CSS", "JavaScript", "React"] },
  { group: "Backend", items: ["Node.js (basic)"] },
  { group: "Tools", items: ["GitHub", "VS Code"] },
];
---

<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>{profile.name} | Portfolio</title>
    <meta name="description" content={`${profile.name} のポートフォリオサイト`} />
    <style>
      :global(html) { scroll-behavior: smooth; }
      :global(body) { margin: 0; font-family: system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji", "Segoe UI Emoji"; }
      a { color: inherit; }
      .wrap { max-width: 980px; margin: 0 auto; padding: 32px 16px 64px; }
      header { display: flex; align-items: center; justify-content: space-between; gap: 16px; position: sticky; top: 0; background: rgba(255,255,255,0.9); backdrop-filter: blur(10px); border-bottom: 1px solid #eee; }
      header .inner { max-width: 980px; margin: 0 auto; padding: 14px 16px; width: 100%; display:flex; align-items:center; justify-content:space-between; gap:16px; }
      nav { display: flex; gap: 12px; flex-wrap: wrap; }
      nav a { text-decoration: none; padding: 6px 10px; border: 1px solid #eee; border-radius: 999px; }
      nav a:hover { border-color: #ccc; }

      .hero { padding: 40px 0 24px; }
      .hero h1 { font-size: 40px; margin: 0 0 8px; }
      .hero p { margin: 0; font-size: 16px; line-height: 1.7; color: #444; }
      .badges { margin-top: 14px; display: flex; gap: 10px; flex-wrap: wrap; }
      .badge { font-size: 13px; border: 1px solid #eee; border-radius: 999px; padding: 6px 10px; color:#333; }

      section { padding: 22px 0; }
      section h2 { font-size: 22px; margin: 0 0 12px; }
      .grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; }
      @media (max-width: 900px) { .grid { grid-template-columns: repeat(2, 1fr); } }
      @media (max-width: 620px) { .grid { grid-template-columns: 1fr; } }

      .card { border: 1px solid #eee; border-radius: 16px; padding: 14px; background: #fff; }
      .card h3 { margin: 0 0 6px; font-size: 16px; }
      .card p { margin: 0 0 10px; font-size: 14px; line-height: 1.6; color: #444; }
      .chips { display:flex; gap: 6px; flex-wrap: wrap; margin: 0 0 10px; }
      .chip { font-size: 12px; border: 1px solid #eee; border-radius: 999px; padding: 4px 8px; }
      .links { display:flex; gap: 10px; flex-wrap: wrap; }
      .links a { font-size: 13px; text-decoration: none; border: 1px solid #eee; border-radius: 10px; padding: 8px 10px; }
      .links a:hover { border-color: #ccc; }

      footer { margin-top: 32px; padding-top: 16px; border-top: 1px solid #eee; color: #666; font-size: 13px; }
      .muted { color: #666; }
    </style>
  </head>

  <body>
    <header>
      <div class="inner">
        <strong>{profile.name}</strong>
        <nav aria-label="site">
          <a href="#about">About</a>
          <a href="#works">Works</a>
          <a href="#skills">Skills</a>
          <a href="#contact">Contact</a>
        </nav>
      </div>
    </header>

    <main class="wrap">
      <div class="hero" id="top">
        <h1>{profile.name}</h1>
        <p class="muted">{profile.role} • {profile.location}</p>
        <p style="margin-top:10px;">{profile.tagline}</p>

        <div class="badges">
          <a class="badge" href={profile.links.github} target="_blank" rel="noreferrer">GitHub</a>
          <a class="badge" href={profile.links.email}>Email</a>
        </div>
      </div>

      <section id="about">
        <h2>About</h2>
        <div class="card">
          <p>
            ここに自己紹介を書きます（例：何が得意か、どんな領域に興味があるか、仕事/学習のスタイルなど）。
            3〜6行くらいで読みやすくまとめるのがコツです。
          </p>
        </div>
      </section>

      <section id="works">
        <h2>Works</h2>
        <div class="grid">
          {works.map((w) => (
            <article class="card">
              <h3>{w.title}</h3>
              <p>{w.description}</p>

              <div class="chips">
                {w.tech.map((t) => <span class="chip">{t}</span>)}
              </div>

              <div class="links">
                {w.demo ? <a href={w.demo} target="_blank" rel="noreferrer">Demo</a> : null}
                <a href={w.repo} target="_blank" rel="noreferrer">GitHub</a>
              </div>
            </article>
          ))}
        </div>
        <p class="muted" style="margin-top:10px;">
          作品が増えたら、/works の一覧ページと詳細ページを追加して整理できます。
        </p>
      </section>

      <section id="skills">
        <h2>Skills</h2>
        <div class="grid">
          {skills.map((s) => (
            <div class="card">
              <h3>{s.group}</h3>
              <p style="margin:0;">
                {s.items.join(" / ")}
              </p>
            </div>
          ))}
        </div>
      </section>

      <section id="contact">
        <h2>Contact</h2>
        <div class="card">
          <p style="margin:0 0 10px;">
            お問い合わせはメールかGitHubからお願いします。
          </p>
          <div class="links">
            <a href={profile.links.email}>Email</a>
            <a href={profile.links.github} target="_blank" rel="noreferrer">GitHub</a>
          </div>
        </div>
      </section>

      <footer>
        © {new Date().getFullYear()} {profile.name}. Built with Astro.
      </footer>
    </main>
  </body>
</html>
