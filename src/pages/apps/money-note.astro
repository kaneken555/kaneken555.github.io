---
const appName = "Money Note";
const tagline = "シンプルな収支管理で続く家計簿";
const description = "Money Noteは、毎日の収支をサッと記録できるシンプルな家計簿アプリです。カテゴリ別の集計や月次レポートで、お金の流れをひと目で把握できます。";
const siteUrl = "https://kaneken555.github.io";
const pageUrl = `${siteUrl}/apps/money-note`;
const ogImage = `${siteUrl}/images/apps/money-note/icon.png`;
const appStoreUrl = "https://apps.apple.com/jp/app/money-note-%E3%82%B7%E3%83%B3%E3%83%97%E3%83%AB%E5%AE%B6%E8%A8%88%E7%B0%BF/id6758237102";

const screenshots = [
  { src: "/images/apps/money-note/01-home.png", alt: "ホーム画面" },
  { src: "/images/apps/money-note/02-add-transaction.png", alt: "収支入力画面" },
  { src: "/images/apps/money-note/03-transactions.png", alt: "取引一覧画面" },
  { src: "/images/apps/money-note/04-pl-report.png", alt: "収支レポート画面" },
  { src: "/images/apps/money-note/05-settings.png", alt: "設定画面" },
];

const features = [
  { icon: "📝", title: "かんたん入力", text: "金額・カテゴリを選ぶだけでサッと記録" },
  { icon: "📊", title: "月次レポート", text: "収入・支出・残高をグラフで確認" },
  { icon: "🏷️", title: "カテゴリ管理", text: "自分に合ったカテゴリを自由にカスタマイズ" },
  { icon: "📅", title: "カレンダー表示", text: "日ごとの収支をカレンダーで一覧" },
  { icon: "🔒", title: "オフライン対応", text: "ネットワーク不要でいつでも記録可能" },
  { icon: "🎨", title: "シンプルUI", text: "余計な機能のない、見やすいデザイン" },
];

const targets = [
  "家計簿を始めたいけど続かなかった方",
  "シンプルに収支だけ管理したい方",
  "毎月の出費を把握したい方",
  "複雑な機能は不要で、手軽に使いたい方",
];

const jsonLd = {
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  name: appName,
  description,
  operatingSystem: "iOS",
  applicationCategory: "FinanceApplication",
  offers: {
    "@type": "Offer",
    price: "0",
    priceCurrency: "JPY",
  },
  url: pageUrl,
  image: ogImage,
};
---

<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>{appName} - {tagline}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={pageUrl} />

    <meta property="og:title" content={`${appName} - ${tagline}`} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:url" content={pageUrl} />
    <meta property="og:type" content="website" />

    <meta name="twitter:card" content="summary" />
    <meta name="twitter:title" content={`${appName} - ${tagline}`} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImage} />

    <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />

    <style is:global>
      html { scroll-behavior: smooth; }
      body { margin: 0; font-family: system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji", "Segoe UI Emoji"; color: #333; }
      a { color: inherit; }
    </style>
    <style>
      .wrap {
        max-width: 640px;
        margin: 0 auto;
        padding: 0 16px 64px;
      }

      /* ── Hero ── */
      .hero {
        text-align: center;
        padding: 48px 0 32px;
      }
      .hero-icon {
        width: 120px;
        height: 120px;
        border-radius: 24px;
        box-shadow: 0 4px 16px rgba(0,0,0,0.10);
      }
      .hero h1 {
        font-size: 32px;
        margin: 20px 0 8px;
      }
      .hero .tagline {
        font-size: 16px;
        color: #444;
        margin: 0 0 24px;
        line-height: 1.7;
      }
      .store-badge {
        display: inline-block;
        border: 1px solid #eee;
        border-radius: 12px;
        padding: 12px 24px;
        text-decoration: none;
        font-size: 15px;
        font-weight: 600;
        transition: border-color 0.2s;
      }
      .store-badge:hover {
        border-color: #ccc;
      }

      /* ── Screenshots ── */
      .screenshots {
        padding: 24px 0;
      }
      .screenshots h2 {
        font-size: 22px;
        margin: 0 0 16px;
      }
      .screenshot-scroll {
        display: flex;
        gap: 12px;
        overflow-x: auto;
        padding-bottom: 12px;
        -webkit-overflow-scrolling: touch;
        scroll-snap-type: x mandatory;
      }
      .screenshot-scroll::-webkit-scrollbar {
        height: 6px;
      }
      .screenshot-scroll::-webkit-scrollbar-thumb {
        background: #ddd;
        border-radius: 3px;
      }
      .screenshot-scroll img {
        width: 200px;
        flex-shrink: 0;
        border-radius: 16px;
        border: 1px solid #eee;
        scroll-snap-align: start;
      }
      @media (max-width: 480px) {
        .screenshot-scroll img {
          width: 160px;
        }
      }

      /* ── Sections ── */
      .section {
        padding: 24px 0;
      }
      .section h2 {
        font-size: 22px;
        margin: 0 0 12px;
      }

      .about-text {
        font-size: 15px;
        line-height: 1.8;
        color: #444;
        margin: 0;
      }

      /* ── Features ── */
      .features-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 12px;
      }
      @media (max-width: 480px) {
        .features-grid {
          grid-template-columns: 1fr;
        }
      }
      .feature-card {
        border: 1px solid #eee;
        border-radius: 16px;
        padding: 16px;
      }
      .feature-icon {
        font-size: 24px;
        margin-bottom: 8px;
      }
      .feature-card h3 {
        font-size: 15px;
        margin: 0 0 4px;
      }
      .feature-card p {
        font-size: 13px;
        color: #666;
        margin: 0;
        line-height: 1.6;
      }

      /* ── Targets ── */
      .target-list {
        list-style: none;
        padding: 0;
        margin: 0;
      }
      .target-list li {
        font-size: 15px;
        line-height: 1.8;
        color: #444;
        padding: 6px 0;
        border-bottom: 1px solid #eee;
      }
      .target-list li:last-child {
        border-bottom: none;
      }
      .target-list li::before {
        content: "✓ ";
        color: #666;
      }

      /* ── CTA ── */
      .cta {
        text-align: center;
        padding: 40px 0;
      }
      .cta p {
        font-size: 15px;
        color: #444;
        margin: 0 0 16px;
      }

      /* ── Footer ── */
      .lp-footer {
        text-align: center;
        padding-top: 16px;
        border-top: 1px solid #eee;
        color: #666;
        font-size: 13px;
      }
      .lp-footer a {
        text-decoration: none;
      }
      .lp-footer a:hover {
        text-decoration: underline;
      }
      .lp-footer .back-link {
        display: inline-block;
        margin-bottom: 12px;
      }
    </style>
  </head>

  <body>
    <main class="wrap">
      <!-- Hero -->
      <div class="hero">
        <img class="hero-icon" src="/images/apps/money-note/icon.png" alt={`${appName} アイコン`} width="120" height="120" />
        <h1>{appName}</h1>
        <p class="tagline">{tagline}</p>
        <a class="store-badge" href={appStoreUrl} target="_blank" rel="noreferrer">
          App Store からダウンロード
        </a>
      </div>

      <!-- Screenshots -->
      <section class="screenshots">
        <h2>スクリーンショット</h2>
        <div class="screenshot-scroll">
          {screenshots.map((s) => (
            <img src={s.src} alt={s.alt} width="1320" height="2868" loading="lazy" />
          ))}
        </div>
      </section>

      <!-- About -->
      <section class="section">
        <h2>アプリ概要</h2>
        <p class="about-text">{description}</p>
      </section>

      <!-- Features -->
      <section class="section">
        <h2>主な機能</h2>
        <div class="features-grid">
          {features.map((f) => (
            <div class="feature-card">
              <div class="feature-icon">{f.icon}</div>
              <h3>{f.title}</h3>
              <p>{f.text}</p>
            </div>
          ))}
        </div>
      </section>

      <!-- Targets -->
      <section class="section">
        <h2>こんな方におすすめ</h2>
        <ul class="target-list">
          {targets.map((t) => (
            <li>{t}</li>
          ))}
        </ul>
      </section>

      <!-- CTA -->
      <div class="cta">
        <p>無料でお試しいただけます</p>
        <a class="store-badge" href={appStoreUrl} target="_blank" rel="noreferrer">
          App Store からダウンロード
        </a>
      </div>

      <!-- Footer -->
      <footer class="lp-footer">
        <a class="back-link" href="/">kaneken555 Portfolio に戻る</a>
        <p>&copy; {new Date().getFullYear()} kaneken555. Built with Astro.</p>
      </footer>
    </main>
  </body>
</html>
